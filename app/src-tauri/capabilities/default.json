{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the local AI coding agent shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
